/// Provides the [command] function.
library command;

const String command = 'Decorate an instance method to make it a command recognised by Backstreets.';

class CommandCollection {
  CommandCollection(this.name);

  String name;
}
